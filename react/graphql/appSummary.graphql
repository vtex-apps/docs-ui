query appSummary($appName: String, $locale: String) @context(provider: "vtex.docs-graphql@2.x"){
  appSummary(appName: $appName, locale: $locale) {
    markdown
    chapterList {
      path
      title
      articles {
        path
        title
        articles {
          path
          title
          articles {
            path
            title
            articles {
              path
            }
          }
        }
      }
    }
  }
}
