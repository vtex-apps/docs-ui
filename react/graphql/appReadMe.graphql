query appReadMe($appName: String, $locale: String) @context(provider: "vtex.docs-graphql@2.x"){
  appReadMe(appName: $appName, locale: $locale) {
    meta {
      title
      description
      version
      tags
      git
      date
    }
    markdown
  }
}
