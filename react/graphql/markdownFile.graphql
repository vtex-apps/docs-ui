query markdownFile($appName: String, $fileName: String, $locale: String) @context(provider: "vtex.docs-graphql@2.x"){
  markdownFile(appName: $appName, fileName: $fileName, locale: $locale) {
    meta {
      title
      description
      tags
      version
      git
    }
    markdown
  }
}
