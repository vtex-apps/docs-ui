query published($appName: String) @context(provider: "vtex.docs-graphql@2.x"){
  appMajors(appName: $appName) {
    latestMajor
    publishedMajors
  }
  appVersions(appName: $appName) {
    latestStable
    publishedVersions
  }
}
